<?xml version="1.0" encoding="utf-8"?>
<!--
    AndroidManifest.xml for Snadder Game
    =====================================
    SDL2-based C++ game with touch input, audio, and gamepad support.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Gamepad/controller support (optional hardware) -->
    <uses-feature
        android:name="android.hardware.gamepad"
        android:required="false" />

    <!-- OpenGL ES 2.0 (required by SDL2 renderer) -->
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <!-- Touchscreen (required for game interaction) -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SnadderGame"
        android:hasCode="true">

        <activity
            android:name="com.snadder.game.SnadderActivity"
            android:alwaysRetainTaskState="true"
            android:configChanges="layoutDirection|locale|orientation|uiMode|screenLayout|screenSize|smallestScreenSize|keyboard|keyboardHidden|navigation"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="portrait">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
